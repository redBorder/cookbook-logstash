filter {
  mutate{
        gsub => ["[message]", "[\n]", ""]
        gsub => ["[message]", "[\\]", ""]
        gsub => ["[message]", "[\/]", ""]
        gsub => ["[message]", "[\%]", ""]
  }
  bulkstats {
        bulkstats_columns_tar_gz => "/share/bulkstats.tar.gz"
  }
} # end of filter 

