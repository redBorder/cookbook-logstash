filter {
  if ![tag] or [tag] == 0 {
    ruby {
      init => '
        require "ipaddr"
        require "yaml"

        begin
          path = "/etc/logstash/sflow_homenets/default.yml"
          raw_data = YAML.safe_load(File.read(path)) || {}
          $sensor_homenets = {}

          raw_data.each do |sensor, networks|
            $sensor_homenets[sensor] = networks.map do |n|
              IPAddr.new(n) rescue nil
            end.compact
          end
        rescue => e
          $sensor_homenets = {}
        end

        def is_internal?(ip_str, homenets)
          return false if ip_str.nil?
          begin
            target = IPAddr.new(ip_str)
            homenets.any? { |net_obj| net_obj.include?(target) }
          rescue
            false
          end
        end
      '
      code => '
        sensor_ip = event.get("peer_ip_src")
        homenets = $sensor_homenets[sensor_ip] || []

        if is_internal?(event.get("ip_src"), homenets)
          event.tag("is_src_internal")
        end

        if is_internal?(event.get("ip_dst"), homenets)
          event.tag("is_dst_internal")
        end
      '
    }
    
    if "is_src_internal" in [tags] and "is_dst_internal" in [tags] {
      mutate { add_field => { "direction" => "internal" } }
    } else if "is_src_internal" in [tags] {
      mutate { add_field => { "direction" => "upstream" } }
    } else if "is_dst_internal" in [tags] {
      mutate { add_field => { "direction" => "downstream" } }
    } else {
      mutate { add_field => { "direction" => "N/A" } }
    }
  } else if [tag] == 1 {
      mutate { add_field => { "direction" => "downstream" } }
  } else if [tag] == 3 {
      mutate { add_field => { "direction" => "internal" } }
  }

  <% if @split_traffic_logstash %>
  if [tag2] {
    ruby {
      init => '
        require "ipaddr"

        $proxy_observations = <%
          return [] unless @proxy_nodes

          # Expected output
          # proxy_observations = {
          #   sensor_ip => [
          #     {
          #       id => x
          #       subnet => x.x.x.x/x
          #     },
          #     {...}
          #   ]
          # }
          
          @proxy_nodes.each do |proxy_node|
            next unless proxy_node[:redborder] && proxy_node[:observation_id_filters]

            filters = []
            proxy_node[:observation_id_filters].map do |filter|
              next unless filter[:observation_id] && filter[:subnet]

              begin
                id = filter[:observation_id]
                subnet = IPAddr.new(filter[:subnet])
                filters.push({id: id, subnet: subnet})
              rescue
                next
              end
            end
            sensor_ip = proxy_node[:ipaddress]
            proxy_observations[sensor_ip] = filters
          end
          proxy_observations
        %>
      '
      code => '
        ip_src = IPAddr.new(event.get("ip_src")) rescue nil
        ip_dst = IPAddr.new(event.get("ip_dst")) rescue nil
        return unless (ip_src || ip_dst)

        target_observation = event.get("tag2")
        sensor_ip = event.get("peer_ip_src")

        observations = $proxy_observations.find { |n| n[:ipaddress] == sensor_ip }
        observations.each do |observation|
          subnet = observation[:subnet]
          if subnet.include?(ip_src) || subnet.include?(ip_dst)
            observation_tag2 = observation[:id] 
            break
          end
        end

        # Replace event
        event.set("tag2", out_tag) if tag2 != observation_tag2
      '
    }
  }
  <% end %>
}
