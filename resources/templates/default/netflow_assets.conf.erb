filter {
  # 1. Map ASSET TYPE ID with asset type name
  translate {
    field => "[client_mac]"
    destination => "[client_asset_type]"
    dictionary_path => "<%= @mac_to_asset_type_file %>"
    override => true
  }
}